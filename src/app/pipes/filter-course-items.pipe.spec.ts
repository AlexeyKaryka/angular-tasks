import { FilterCourseItemsPipe } from './filter-course-items.pipe';

describe('FilterCourseItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCourseItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
